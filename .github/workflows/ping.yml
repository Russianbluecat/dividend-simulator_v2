name: Keep Streamlit Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # 10분마다 실행
  workflow_dispatch:         # 필요시 수동 실행도 가능

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Streamlit app
        run: curl -I -s https://dividend-reinvest-simul.streamlit.app/ > /dev/null
